let top = {
  init: function() {
    console.log('[init] ble')
  },

  check_id_localDB: id => {
    console.log(c.cyan + '[start]', 'checl_id_localDB()')

    NativeStorage.keys(test.console(elm, 'success'))
  },
}

export default top
